import { City } from "./City.models";

export class Country{
    id:number;
    name:string;

    setId(id:number)
    {
        this.id=id;
    }
    setName(name:string)
    {
        this.name=name;
    }
    
}